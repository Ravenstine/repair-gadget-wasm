/**
 * A sprite is basically the instance of a cast member.
 *
 * It can have behaviors, which are usually scripts.
 *
 * @typedef Sprite
 *
 * @property {CastMember} castMember
 * @property {Array<Frame>} frames
 * @property {Array<Keyframe>} keyframes
 * @property {Number} startFrame
 * @property {Number} endFrame
 */